return {
  {
    "Exafunction/codeium.vim",
    event = "BufEnter",
    config = function()
      -- Disable default bindings if you want to customize them
      -- vim.g.codeium_disable_bindings = 1
    end
  }
}
